source("renv/activate.R")


if (interactive()) {
  suppressWarnings(suppressPackageStartupMessages({
    library(usethis)
    ui_done("Package {ui_value('usethis')} loaded.")
    library(checkmate)
    ui_done("Package {ui_value('checkmate')} loaded.")
    library(devtools)
    ui_done("Package {ui_value('devtools')} loaded.")
    library(testthat)
    ui_done("Package {ui_value('testthat')} loaded.")
  }))
}

message("Buon lavoro, oggi è una splendida giornata!!!")
